---
import type { ImageMetadata } from 'astro';
import HeroImage from './HeroImage.astro';
interface Props {
  image?: ImageMetadata;
  alt?: string;
  caption?: string;
  eager?: boolean;
}
const props = Astro.props;
---

<HeroImage {...props} class="aspect-[3/2]" />
