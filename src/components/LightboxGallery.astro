---
import PhotoGallery from './PhotoGallery.astro';
const { photos } = Astro.props;
---

<!-- Kept JavaScript-free for the first release; full-size links can be added with personal photos. -->
<PhotoGallery {photos} />
